---
name: clone
description: "Клонирование всех доступных GitLab репозиториев с сохранением структуры namespace"
category: devops
complexity: medium
mcp-servers: []
personas: []
---

# /gitlab:clone - Клонирование GitLab репозиториев

Массовое клонирование всех доступных GitLab проектов с сохранением полной структуры namespace/group/subgroup.

## Triggers
- Первичное клонирование всех репозиториев для аудита
- Обновление локальной копии репозиториев
- Подготовка к поиску чувствительных данных

## Usage
```
/gitlab:clone [путь_до_папки]
```

**Аргументы:**
- `[путь_до_папки]` — полный путь куда клонировать (опционально)
- Если путь не указан, используется: `/Users/alexeyprudkin/ops/gitlab_audit`

## Директория по умолчанию

```bash
# Определяется относительно $HOME (не hardcoded)
DEFAULT_DIR="${1:-$HOME/ops/gitlab_audit}"
```

## Требования

- **glab** — GitLab CLI с настроенной авторизацией
- Достаточно места на диске
- Доступ к GitLab инстансу

## Behavioral Flow

Когда пользователь вызывает `/gitlab:clone [path]`:

1. **Определение директории**
   ```
   Если указан path → использовать path
   Если path не указан → использовать /Users/alexeyprudkin/ops/gitlab_audit
   ```

2. **Проверка окружения**
   ```bash
   # Проверить авторизацию GitLab
   glab auth status

   # Проверить/создать директорию
   mkdir -p "<target_dir>"
   ```

3. **Получение списка проектов**
   ```bash
   # Посмотреть сколько проектов доступно
   glab repo list --all --per-page 100 | wc -l
   ```

4. **Клонирование с сохранением namespace**
   ```bash
   cd "<target_dir>"

   # Получить список всех проектов
   PROJECTS=$(glab repo list --all --per-page 100 | awk '{print $1}')

   # Клонировать с пропуском уже существующих (--skip-existing)
   # -p (--preserve-namespace): КРИТИЧНО - сохраняет полную структуру групп
   # --depth 1: shallow clone (только последний коммит, экономит время и место)
   echo "$PROJECTS" | while read -r repo; do
     REPO_PATH="<target_dir>/$(echo "$repo" | tr '/' '/')"
     if [ -d "$REPO_PATH/.git" ]; then
       echo "⏭️ Пропускаю (уже склонирован): $repo"
     else
       echo "📥 Клонирую: $repo"
       glab repo clone "$repo" -p -- --depth 1
     fi
   done
   ```

   **Важно:** Флаг `-p` должен идти ПЕРЕД `--`, чтобы относиться к glab, а не к git.

### 4.1 Возобновление прерванного клонирования

Если процесс был прерван, просто запусти команду повторно — уже склонированные репозитории будут пропущены автоматически.

5. **Проверка результата**
   ```bash
   # Посчитать склонированные репозитории
   find "<target_dir>" -type d -name ".git" | wc -l

   # Показать структуру верхнего уровня
   ls -la "<target_dir>"
   ```

6. **Вывод отчёта**
   ```
   ## Результат клонирования

   Директория: <target_dir>
   Склонировано репозиториев: N

   Структура групп:
   - group1/ (M проектов)
   - group2/ (K проектов)
   ```

## Структура результата

Клонирование сохраняет полный путь namespace из GitLab:

```
/Users/alexeyprudkin/ops/gitlab_audit/
├── ops-projects/
│   ├── aws-ec2/
│   ├── sverka/
│   └── win/
│       ├── automation/
│       │   └── vacations-group-sync/
│       ├── bots/
│       │   └── tg-jira-tasks/
│       └── powershell/
├── devops-projects/
│   └── ...
└── security-archive/
    └── security_private/
        └── appsec/
            └── codeql-paysend-security-pack/
```

## Примеры использования

### Клонировать в директорию по умолчанию
```
/gitlab:clone
```

### Клонировать в указанную директорию
```
/gitlab:clone /Users/alexeyprudkin/projects/audit-2026
```

### Обновить существующие репозитории
```
/gitlab:clone /Users/alexeyprudkin/ops/gitlab_audit
```
(glab пропустит уже существующие репозитории)

## Обработка ошибок

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `glab: command not found` | glab не установлен | `brew install glab` |
| `not logged in` | Нет авторизации | `glab auth login` |
| `permission denied` | Нет доступа к проекту | Проект приватный, нужны права |
| `already exists` | Репозиторий уже склонирован | Пропускается автоматически |

## Советы

- **Первый запуск** занимает время — много репозиториев
- **--depth 1** экономит ~80% места и времени
- **Повторный запуск** безопасен — существующие репо пропускаются
- После клонирования используй `/gitlab:audit` для поиска

## Связанные команды

- `/gitlab:audit <pattern>` — поиск по склонированным репозиториям
