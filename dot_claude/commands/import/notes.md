---
name: notes
description: "Импорт заметок из Apple Notes в Obsidian vault через Importer плагин"
category: utility
complexity: medium
mcp-servers: []
personas: []
---

# /import:notes - Импорт Apple Notes

Импорт заметок из Apple Notes через официальный Obsidian Importer плагин с последующим распределением по базе знаний PARA.

## Triggers
- Миграция с Apple Notes на Obsidian
- Первичный импорт всех заметок
- Распределение импортированных заметок по PARA

## Usage
```
/import:notes [--setup] [--organize] [--organize N] [--auto] [--preview]
```

**Аргументы:**
- `--setup` — проверить/установить Importer плагин и показать инструкции
- `--organize` — распределить уже импортированные заметки по PARA
- `--organize N` — распределить N заметок
- `--auto` — автоматическое распределение без подтверждения
- `--preview` — показать что будет обработано

**Примеры:**
```
/import:notes --setup        # подготовка к импорту
/import:notes --organize 5   # распределить 5 заметок
/import:notes --organize     # распределить все новые
```

## Workflow

### Шаг 1: Импорт через Obsidian Importer (GUI)

1. **Установка плагина** (один раз):
   - Settings → Community Plugins → Browse
   - Найти "Importer" → Install → Enable

2. **Запуск импорта**:
   - Cmd+P → "Importer: Open"
   - File format: **Apple Notes**
   - Output folder: `00 Inbox/Apple Notes Import/`
   - ☑️ Omit first line as title (Apple Notes uses it as title)
   - Click **Import**
   - При запросе: Open → выбрать `group.com.apple.notes`

3. **Дождаться завершения** импорта

> **Важно:** Locked notes (с паролем) будут пропущены. Разблокируй их перед импортом.

### Шаг 2: Распределение по PARA (Claude)

После импорта вызови:
```
/import:notes --organize
```

## Behavioral Flow

Когда пользователь вызывает `/import:notes`:

### 1. Проверка режима

```
Если --setup → показать инструкции по установке Importer
Если --organize [N] → распределить N (или все) заметки
Если без аргументов → показать статус и предложить действия
```

### 2. Для --setup

Проверить установлен ли Importer:
```bash
# Obsidian плагины находятся в директории vault
ls "$VAULT_PATH/.obsidian/plugins/obsidian-importer/" 2>/dev/null || echo "Плагин не установлен"
```

**Примечание:** `$VAULT_PATH` — текущая рабочая директория Claude Code (обычно корень vault).

Если не установлен — показать инструкции:
```
## Установка Obsidian Importer

1. Открой Obsidian Settings (Cmd+,)
2. Community Plugins → Browse
3. Найди "Importer" → Install → Enable
4. Cmd+P → "Importer: Open"
5. Выбери "Apple Notes" и нажми Import

После импорта запусти: `/import:notes --organize`
```

### 3. Для --organize [N]

a. **Сканирование папки импорта**
```bash
ls -la "00 Inbox/Apple Notes Import/"
```

b. **Фильтрация необработанных**
Найти файлы БЕЗ `distributed: true` в frontmatter:
```bash
# Файлы без метки distributed
for f in "00 Inbox/Apple Notes Import/"*.md; do
  grep -q "distributed:" "$f" || echo "$f"
done
```

c. **Для каждого файла (до лимита N):**

1. Прочитать содержимое
2. Определить тип и целевую папку (см. таблицу ниже)
3. Показать через AskUserQuestion:
   ```
   📄 Заметка: "Название"

   Содержимое (превью):
   ---
   [текст]
   ---

   Предложение:
   - Тип: wiki
   - Папка: 30 Wiki/
   - Теги: [tag1, tag2]

   Действие:
   1. Принять
   2. Изменить
   3. Пропустить
   ```

4. После подтверждения:
   - Обновить frontmatter:
     ```yaml
     ---
     created: YYYY-MM-DD
     updated: YYYY-MM-DD
     type: <тип>
     tags: [<теги>, imported]
     source: apple-notes
     distributed: true
     distributed_date: YYYY-MM-DD
     ---
     ```
   - Переместить в целевую папку
   - Удалить из папки импорта

d. **Финальный отчёт**
```
✅ Распределение завершено

Обработано: 5 из 15
- Wiki: 2
- Cheatsheets: 1
- Bookmarks: 1
- Пропущено: 1

Осталось: 10
```

e. **Git commit**
```bash
git add .
git commit -m "Distribute N notes from Apple Notes import"
git push
```

## Типы контента и целевые папки

| Контент | Тип | Целевая папка |
|---------|-----|---------------|
| Справочник, инструкция, how-to | `cheatsheet` | `60 Resources/Cheatsheets/` |
| Код, скрипты, примеры | `snippet` | `60 Resources/Code Snippets/` |
| Ссылки, статьи | `bookmark` | `60 Resources/Bookmarks/` |
| Концепции, знания, теория | `wiki` | `30 Wiki/` |
| Активная задача с дедлайном | `project` | `20 Projects/` |
| Рабочая задача, тикет | `work-task` | `50 Areas/Work/Active/` |
| Медицинский документ | `medical` | `50 Areas/Health/Medical/` |
| Личные заметки, мысли | `note` | `50 Areas/Life/` |

## Признаки для автоопределения типа

### cheatsheet
- Содержит команды (`$`, `>`), code blocks
- Слова: "команды", "шпаргалка", "howto", "tutorial"
- Списки с техническими инструкциями

### wiki
- Объясняет концепцию или технологию
- Определения, теория, архитектура
- Ссылки на документацию

### snippet
- Много кода (>50% контента)
- Файлы: `.py`, `.js`, `.sh` в тексте
- Слова: "функция", "скрипт", "код"

### bookmark
- Содержит URL в начале
- Краткое описание + ссылка
- Слова: "статья", "почитать", "посмотреть"

### work-task
- Упоминание JIRA, тикетов
- Технические задачи
- Слова: "баг", "фикс", "деплой"

## Obsidian Importer: что поддерживается

| Контент Apple Notes | Результат в Obsidian |
|---------------------|----------------------|
| Текст | Markdown |
| Таблицы | Markdown tables |
| Изображения | Embedded images |
| Рисунки | Images |
| Сканы документов | PDF или изображения |
| Ссылки (iOS 17+) | Wikilinks |
| Заблокированные заметки | ❌ Пропускаются |

## Структура папок

```
00 Inbox/
├── Apple Notes Import/     # Папка импорта Importer
│   ├── Note 1.md          # Импортированные заметки
│   ├── Note 2.md
│   └── attachments/       # Вложения
└── Import/                # Для ручного импорта (legacy)
```

## Примеры использования

### Первый импорт
```
/import:notes --setup
```
Показывает инструкции по установке Importer.

### Распределить 5 заметок
```
/import:notes --organize 5
```

### Автоматическое распределение всех
```
/import:notes --organize --auto
```

### Только посмотреть что есть
```
/import:notes --preview
```

## Обработка ошибок

| Ситуация | Действие |
|----------|----------|
| Importer не установлен | Показать инструкции по установке |
| Папка импорта пуста | Показать как запустить импорт в Obsidian |
| Locked notes пропущены | Напомнить разблокировать перед импортом |
| Вложения не найдены | Проверить папку attachments |

## Советы

- **Первый импорт:** используй `--setup` для проверки окружения
- **Большой объём:** импортируй через Importer, затем `--organize 10` порциями
- **Автоматизация:** `--auto` для больших импортов с последующей ручной проверкой
- **Locked notes:** разблокируй в Apple Notes перед импортом

## Техническая информация

### Расположение базы данных Apple Notes

Apple Notes хранит данные в SQLite базе:
```bash
# macOS 10.15+ (Catalina и новее)
~/Library/Group Containers/group.com.apple.notes/NoteStore.sqlite

# Для проверки доступа
ls -la ~/Library/Group\ Containers/group.com.apple.notes/
```

**Важно:** Obsidian Importer автоматически находит эту базу. Ручной доступ не требуется.

### Требования к версиям

- **macOS:** 10.15+ (Catalina) для корректного импорта
- **Obsidian:** 1.0.0+
- **Importer plugin:** 1.0.0+ (проверить в Settings → Community Plugins)

## Источники

- [Import from Apple Notes - Obsidian Help](https://help.obsidian.md/import/apple-notes)
- [Obsidian Importer Blog Post](https://obsidian.md/blog/apple-notes-export/)

## Связанные команды

- `/obs:inbox` — обработка заметок в Inbox
- `/obs:inbox-scan` — OCR сканирование изображений
