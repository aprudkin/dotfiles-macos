---
name: find
description: "Умный поиск по vault: контент, теги, frontmatter, ссылки"
category: utility
complexity: medium
mcp-servers: []
personas: []
---

# /obs:find - Умный поиск по Vault

Поиск заметок по содержимому, тегам, frontmatter полям и wikilinks.

## Triggers
- Поиск заметок по ключевым словам
- Поиск по тегам или типам
- Поиск связанных заметок

## Usage
```
/obs:find <query> [--tag <tag>] [--type <type>] [--folder <folder>] [--links-to <note>] [--links-from <note>] [--created <date>] [--limit N]
```

**Аргументы:**
- `<query>` — текст для поиска (поддерживает regex)
- `--tag <tag>` — фильтр по тегу (без #)
- `--type <type>` — фильтр по типу (daily, project, wiki, work-task, etc.)
- `--folder <folder>` — искать только в папке
- `--links-to <note>` — заметки со ссылками НА указанную
- `--links-from <note>` — заметки НА КОТОРЫЕ ссылается указанная
- `--created <date>` — по дате создания (YYYY-MM-DD, last-week, last-month)
- `--limit N` — ограничить результаты (по умолчанию: 20)

## Behavioral Flow

Когда пользователь вызывает `/obs:find <query>`:

### 1. Парсинг запроса

Определить тип поиска:
- Простой текст → поиск в содержимом
- `#tag` → поиск по тегу
- `type:wiki` → поиск по frontmatter type
- `[[Note]]` → поиск ссылок

### 2. Выполнение поиска

#### Поиск по содержимому
```bash
# Использовать grep/ripgrep для поиска
grep -rn --include="*.md" "<query>" . | grep -v ".obsidian"
```

#### Поиск по тегам
```bash
# В frontmatter (tags: [tag1, tag2])
grep -rn "tags:.*<tag>" --include="*.md" .

# В тексте (#tag)
grep -rn "#<tag>" --include="*.md" .
```

#### Поиск по frontmatter
```bash
# type: wiki
grep -rn "^type: <type>" --include="*.md" .
```

#### Поиск по ссылкам
```bash
# Заметки ссылающиеся на Note
grep -rn "\[\[Note\]\]" --include="*.md" .

# Заметки на которые ссылается Note
# Прочитать Note.md и извлечь все [[...]]
```

### 3. Форматирование результатов

```markdown
## Результаты поиска: "<query>"

Найдено: **15 заметок**

### 1. [[Project Alpha]]
📁 `20 Projects/Project Alpha.md`
📅 2026-01-10 | 🏷️ #project #work

> ...контекст с **выделенным** совпадением...

---

### 2. [[API Documentation]]
📁 `30 Wiki/API Documentation.md`
📅 2025-12-15 | 🏷️ #wiki #api

> ...контекст...

---

[Показать ещё 13 результатов]
```

### 4. Интерактивные действия

После показа результатов предложить:
- Открыть заметку (показать полное содержимое)
- Редактировать заметку
- Показать связи заметки
- Уточнить поиск

## Примеры использования

### Простой поиск
```
/obs:find kubernetes
```
Найти все упоминания "kubernetes".

### Поиск по тегу
```
/obs:find --tag project
```
Все заметки с тегом #project.

### Поиск по типу
```
/obs:find --type wiki
```
Все wiki статьи.

### Комбинированный поиск
```
/obs:find docker --type cheatsheet --folder "60 Resources"
```
Cheatsheets про docker в Resources.

### Поиск связей
```
/obs:find --links-to "Claude Code Skills"
```
Все заметки ссылающиеся на Claude Code Skills.

### Поиск по дате
```
/obs:find --created last-week
```
Заметки созданные за последнюю неделю.

### Regex поиск
```
/obs:find "TODO|FIXME|XXX"
```
Найти все TODO-маркеры.

## Особенности

### Умный поиск
- Если query начинается с `#` — поиск по тегу
- Если query в формате `key:value` — поиск по frontmatter
- Если query в `[[...]]` — поиск по ссылкам
- Иначе — полнотекстовый поиск

### Ранжирование результатов
1. Точное совпадение в заголовке
2. Совпадение в frontmatter
3. Совпадение в содержимом
4. По дате изменения (новые выше)

### Исключения
Автоматически исключаются:
- `.obsidian/`
- `node_modules/`
- `.git/`

## Обработка ошибок

| Ситуация | Действие |
|----------|----------|
| Пустой query | Показать справку по использованию |
| Нет результатов | Предложить расширить поиск |
| Слишком много результатов | Предложить уточнить фильтры |

## Связанные команды

- `/obs:cleanup` — диагностика vault
- `/obs:rename` — переименование заметок
- `/obs:retag` — массовая замена тегов
