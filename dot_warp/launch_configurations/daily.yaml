# Daily startup configuration
---
name: Daily
windows:
  - tabs:
      - title: claude
        color: purple
        layout:
          cwd: ~
          commands:
            - exec: claude
      - title: maintenance
        color: green
        layout:
          cwd: ~
          commands:
            - exec: task backup && task update
